services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: coastal
      POSTGRES_PASSWORD: coastal
      POSTGRES_DB: coastal
    ports:
      - "5433:5432"
    volumes:
      - coastal_pg:/var/lib/postgresql/data

volumes:
  coastal_pg:
